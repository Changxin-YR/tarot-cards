# 塔罗灵感牌项目规则

本文件适用于项目根目录及全部子目录。

## 项目边界

- 项目类型：HarmonyOS Stage 模型 ArkTS/ArkUI 单机应用。
- 业务源码：`entry/src/main/ets/`。
- 页面位于 `pages/`，复用 UI 位于 `components/`，领域和系统能力位于 `services/`，持久化位于 `repositories/`，模型位于 `models/`，公共令牌与工具位于 `common/`。
- 应用资源：`entry/src/main/resources/`。
- 不修改项目范围外的文件或相邻工程。

## 产品约束

- 抽牌、牌库、解读、记录和设置必须离线可用。
- 不加入登录、广告、会员、付费入口或无关权限。
- 用户问题、心情、笔记、历史和收藏仅保存在本机。
- 小艺只作为用户主动确认后的可选增强，不得阻断离线流程。
- 塔罗解读用于娱乐与自我反思，不提供确定性医疗、法律、投资或命运判断。

## 标准流程

1. 开始前阅读 `README.md`、`tasks.md`、`changes.md`、`design.md` 和 `design-qa.md`。
2. 在 `tasks.md` 登记任务，状态只使用 `pending`、`in_progress`、`done`、`blocked`。
3. 业务行为遵循测试先行；配置与生成型资源除外。
4. 页面不直接操作数据库，抽牌和解读逻辑不写入 UI 组件。
5. 用户可见文本放入字符串资源，视觉值集中到设计令牌或资源。
6. 完成阶段后执行静态门禁和 HarmonyOS 构建。
7. 只有观察到的设备结果才能写入 `design-qa.md`。

## ArkTS 与 ArkUI

- 保持声明式 UI 合法，使用明确 ArkTS 类型。
- `@Builder` 内不写普通临时变量和不受支持的语句。
- 路由、资源名和 `$r('app.xxx.xxx')` 引用必须真实存在。
- 支持 phone 与 tablet；内容在平板上限制最大宽度。
- 图标按钮同时提供文字或无障碍说明，点击区域至少 48vp。

## 安全

- 不写入或输出签名口令、私钥、令牌、证书秘密或真实用户数据。
- 不复制相邻项目的签名配置。
- 不提交构建缓存、IDE 本地配置和临时文件。

