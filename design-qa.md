# 设计 QA

状态只使用 `passed`、`blocked` 或明确失败说明。

## 2026-08-03 API 22 应用图标验收

- `passed`：API 22 模拟器重新安装最终 debug HAP 后，桌面“塔罗灵感牌”显示新的紫金塔罗牌图标，系统圆角遮罩完整，未观察到外围白边、白色四角或主体异常裁切。
- `passed`：桌面图标边界为 `[400,248][604,452]`，名称“塔罗灵感牌”完整显示；设备证据为 `docs/qa/screenshots/2026-08-03-app-icon-launcher-final.jpeg`。
- `passed`：AppScope 与 entry 源图标均为相同的 1024×1024 RGB PNG，Hvigor 打包资源为 512×512；标准门禁、debug/ohosTest 构建和设备端 72 项 Hypium 通过。

## 2026-08-02 占卜流程退出入口验收

- `passed`：静态回归门禁确认牌阵选择、问题填写、洗牌、选牌、翻牌和结果页均调用同一文字退出按钮，点击区域为 48vp，结果页入口位于“再抽一次”之后。
- `passed`：退出行为的回归契约覆盖牌局临时状态清理和 `selectRoot('home')` 导航栈复位；debug/ohosTest HAP 构建及 API 22 设备端 72 项 Hypium 通过。
- `blocked`：本轮按快速交付要求未逐页执行退出按钮的设备点击与截图验收，不记录未观察到的视觉结论。

## 2026-08-02 API 22 双主题牌面与解读 V3 验收

- `passed`：API 22 phone 实际观察首页、牌库、问题、牌阵、洗牌和选牌页面，顶部内容均位于状态栏下方，底部 TabBar/操作区均位于系统手势区上方。
- `passed`：牌库两套主题牌面保持 3:5 比例，无固定宽高拉伸；网格与横向牌列均未显示侧边滚动条。证据为 `docs/qa/screenshots/2026-08-02-v3-catalog-no-bars.jpeg`。
- `passed`：顶部“灵感记录”菜单覆盖在页面内容上方，展开前后主体位置不变。证据为 `docs/qa/screenshots/2026-08-02-v3-menu-overlay.jpeg`。
- `passed`：抽牌问题、牌阵选择、洗牌和选牌页面布局协调且安全区正常。证据为 `docs/qa/screenshots/2026-08-02-v3-draw-page.jpeg`、`2026-08-02-v3-mode.jpeg`、`2026-08-02-v3-shuffle-page.jpeg` 和 `2026-08-02-v3-select-page.jpeg`。
- `passed`：设备端 Hypium 72 项全部通过，覆盖多牌关系、澄清牌作用与释义、TR4 保存及 TR3 兼容恢复。
- `blocked`：两套宝剑与星币合计 56 张新牌面尚无可用生成来源；tablet、横屏、三键导航和字体放大未在对应设备配置下观察。

## 2026-08-02 API 22 顶部菜单浮层验收

- `passed`：菜单关闭和展开时，“上午好”的设备边界均为 `[56,420][266,502]`，“每日一抽”的设备边界均为 `[520,769][800,851]`，页面主体没有发生位移。
- `passed`：“灵感记录”菜单覆盖在 `[721,381][1233,549]`，位于页面内容上方且保持可点击，没有改变公共 Header 高度。
- `passed`：11 个页面均使用同一公共 Header；静态回归门禁禁止恢复动态 Header 高度，并要求菜单保持覆盖层定位。
- `passed`：设备证据为 `docs/qa/screenshots/2026-08-02-menu-overlay-home.jpeg`；标准门禁、debug HAP 与 `entry@ohosTest` HAP 构建通过。

## 2026-08-02 API 22 全页面系统安全区复核

- `passed`：API 22 模拟器（6.0.0.130，1320x2856）重新安装本轮 debug HAP 后，实际观察首页、牌库、设置、问题、牌阵选择、洗牌、选牌和翻牌页，页面标题均完整位于系统状态栏下方。
- `passed`：翻牌页系统状态栏边界为 `y=0..136`，标题边界为 `y=219..313`，两者无重叠；底部操作按钮止于 `y=2702`，系统手势区从 `y=2758` 开始，保留 56px 间距。
- `passed`：带 TabBar 的首页、牌库、设置与问题页中，Tab 图标和文字均位于系统手势区上方；无 TabBar 的牌阵选择、洗牌、选牌和翻牌页底部操作区域未被系统导航切割。
- `passed`：设备证据为 `docs/qa/screenshots/2026-08-02-top-bottom-safe-home.jpeg` 和 `docs/qa/screenshots/2026-08-02-top-bottom-safe-reveal.jpeg`。
- `passed`：全页面安全区静态回归门禁、debug HAP 与 `entry@ohosTest` HAP 构建均通过。
- `blocked`：tablet、横屏和三键导航配置未在本轮对应设备上观察。

## 2026-08-02 API 22 牌库底部导航安全区验收

- `passed`：API 22 模拟器（6.0.0.130，1320×2856）安装本轮 debug HAP 后，首页与牌库四项 Tab 图标和文字均完整显示在系统手势条上方。
- `passed`：牌库卡牌网格的可视区域终止于应用 TabBar 上方，系统手势条位于独立底部安全区，没有再切割 TabBar。
- `passed`：设备证据为 `docs/qa/screenshots/2026-08-02-bottom-nav-catalog.jpeg`；底部导航布局回归门禁、debug HAP 和 `entry@ohosTest` HAP 编译均通过。
- `blocked`：tablet、横屏和三键导航配置未在本轮对应设备上观察。

## 2026-08-01 API 22 安全区、导航与参与式抽牌验收

- `passed`：API 22、1320×2856 模拟器首页内容从状态栏下方开始，四项底部导航位于系统手势条上方，导航文字和手势指示器无重叠。
- `passed`：首页顶部显示返回按钮和三点按钮；三点首次点击仅展开“灵感记录”菜单项，点击菜单项后才进入记录页，记录页返回按钮回到首页。
- `passed`：主页第一次系统返回显示“再次返回以退出”，1.5 秒内第二次返回显示“退出应用？”原生确认框；取消后应用保持打开。
- `passed`：首页在本轮设备时间显示“下午好”，设备端边界测试覆盖六个问候时段及越界小时归一化。
- `passed`：底部导航只有“首页、占卜、牌库、我的”四项，记录不再占用底部导航位置。
- `passed`：单牌实际依次经过问题、牌阵、三次洗牌、16 张候选牌选一、确认、翻牌和完整解读，未直接显示结果；选中态、确认按钮和底部手势区无重叠。
- `passed`：单牌个人成长解读引用当前场景，逆位“权杖国王”同时给出已有能力、可调整空间和足够小的行动建议，没有确定性承诺。
- `passed`：三牌翻牌页按三列显示“过去、现在、未来趋势”；七牌翻牌页按 4+3 显示七个牌位，牌面、牌位标签和底部操作按钮无重叠或截断。
- `passed`：设备端 Hypium 63 项全部通过，覆盖问候边界、真实上一层返回、主页双返回窗口、三次洗牌门槛、确认锁定、逐张翻牌、问题语义优先和建设性逆位解读。
- `blocked`：tablet、横屏和字体放大未在对应设备配置下观察。

## 2026-07-31 API 22 原生运行与修复验收

- `passed`：API 22（HarmonyOS 6.0.2 模拟器 6.0.0.130）原生安装、启动、首页/问题页/牌阵/洗牌/结果/保存/记录页核心流程可用，无崩溃。
- `passed`：API 22 设备端 Hypium 51 项全部通过，覆盖新增的近期主题提取、SpreadType 归一化与安全检测容错。
- `passed`：保存记录后再次抽牌，解读正确引用近期主题（来自新保存记录的牌关键词）。
- `passed`：每日一抽路径保存记录，`spreadType` 归一化为 `single`，数据结构与牌阵枚举一致。
- `passed`：高风险词检测支持带空格与大小写变体（单元测试验证）。
- `blocked`：`uitest inputText` 无法向 ArkUI TextArea 注入中文（模拟器输入法限制），安全检测容错以单元测试为准；tablet、横屏与字体放大无对应设备证据。

## 2026-07-31 API 22 降级验收

- `passed`：降级包的构建元数据中 compatible 与 target 均为 API 22。
- `passed`：API 22 目标配置下 ArkTS 类型检查、debug HAP、ohosTest HAP、资源校验和标准门禁通过。
- `passed`：降级包在 API 24 phone 模拟器安装、启动成功，设备端 44 项 Hypium 测试全部通过；该结果仅代表向上兼容冒烟。
- `blocked`：当前开发机没有 API 22 SDK、真机或模拟器，HarmonyOS 6.0.2 API 22 原生运行验收尚无设备证据。

## 2026-07-31 第二轮 API 24 phone 模拟器阶段验收

- `passed`：浅色月光紫首页、五项底部导航和安全区在 1320×2856 截图中无重叠。
- `passed`：设置页同时显示月影花庭与星璃穹顶，选中态清晰；切换星璃穹顶后首页牌背立即更新。
- `passed`：问题页支持 300 字输入，问题后进入包含单牌、三张时间流、关系洞察和七张进阶牌阵的选择页。
- `passed`：关系洞察进入星璃穹顶洗牌页并生成三张不重复牌，结果展示位置、牌名和正逆位。
- `passed`：结果页可新增一张不与原牌组重复的澄清牌，本地解读随上下文更新。
- `passed`：七张进阶牌阵在 1320×2856 下按 4+3 排列，位置、牌名、正逆位和正文均无重叠或截断，多牌标题已统一为“牌阵解读/牌阵联系”。
- `passed`：连续新增三张不重复澄清牌后，第四张入口不再显示，原牌组和澄清牌布局保持稳定。
- `passed`：安装最新 debug HAP 后保存星璃穹顶单牌记录，按钮状态变为“已保存”；强停并重启应用后，记录页恢复该条记录及保存时的星璃牌面。
- `passed`：从仅含旧 `readingRecordsV2` 键的两条记录启动时记录页可正常加载；首次保存写入统一 `readingCollectionsV1`，保存后追加一张澄清牌再次保存并强停重启，记录页仍为三条总记录，统一快照中的新记录保持同一 ID，并包含星璃穹顶主牌与独立澄清牌。
- `passed`：最新 debug HAP 中单条删除、清空全部记录和清空全部本地数据均显示包含后果说明、取消和破坏性确认操作的原生对话框；验收时均选择取消，未删除测试数据。
- `passed`：星璃穹顶大阿卡纳与权杖小阿卡纳目录缩略图保持完整边框和一致比例，中文牌名与关键词未截断。
- `blocked`：tablet、横屏和 1.3× 字体未在对应设备配置下观察。

## 2026-07-30 phone 模拟器验收

- `passed`：API 24 phone 模拟器安装、启动与前台稳定运行。
- `passed`：首页、洗牌、结果、牌库和历史记录视觉截图已保存到 `docs/qa/screenshots/`。
- `passed`：每日抽牌显示真实牌面、正逆位和简体中文个性化解读。
- `passed`：历史保存后强停应用并重启，记录仍存在。
- `passed`：牌义详情从顶部打开，收藏入口、简体牌义和本地笔记区可用。
- `passed`：缺少 Agent Framework 系统 HSP 的设备不加载小艺组件，离线核心不崩溃并显示明确状态。
- `blocked`：tablet 运行截图；当前未提供可用平板设备或模拟器证据。
- `blocked`：字体放大、横屏与完整无障碍审查；尚未在对应系统配置下执行。
- `blocked`：小艺正式拉起；等待正式智能体 ID、平台侧可调试状态和包含 `agentKitHsp` 的受支持设备。
