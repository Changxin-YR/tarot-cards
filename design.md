# 设计说明

完整产品设计规格位于：

- `docs/superpowers/specs/2026-07-30-tarot-inspiration-design.md`

## 当前架构

- `pages/`：页面编排、路由和页面局部状态。
- `components/`：无持久化副作用的复用 UI。
- `services/`：牌库、抽牌、问题分类、安全过滤、解读、音效、振动和小艺适配。
- `repositories/`：Preferences 与 RDB 的唯一访问层。
- `models/`：稳定 ID、牌义、抽牌、解读和记录模型。
- `common/`：设计令牌、路由、格式化与校验。

## 视觉

深蓝星夜底色、金色细线、紫色卡片和米白文字。优先保证安全区、信息层级、阅读性、触控尺寸、中文长文本和 phone/tablet 响应式布局。

## 数据与隐私

核心数据只保存在设备本地。小艺增强必须由用户主动确认，并只传递当前问题、抽牌和本地摘要。

